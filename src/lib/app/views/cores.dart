import 'package:flutter/material.dart';

// Paleta de cores padrão do app — centraliza todos os tokens de cor
class AppColors {
  static const azulEscuro     = Color(0xFF0D2A7A); // cor principal — AppBar, botões, ícones
  static const azulMedio      = Color(0xFF1A2E5A); // variação escura do azul — BottomNav ativo
  static const azulClaro      = Color(0xFFEAF0FB); // fundo suave azulado — chips e avatares
  static const brancoCard     = Color(0xFFEDEDED); // fundo de cards claros
  static const cinzaCampo     = Color(0xFFD9D9D9); // fundo de campos de formulário
  static const azulBotao      = Color(0xFF0D2A7A); // alias do azulEscuro para botões
  static const vermelho       = Color(0xFFC40000); // alertas, cancelamentos e rebaixamentos
  static const cinzaTexto     = Color(0xFF777777); // texto secundário e ícones discretos
  static const cinzaEscuro    = Color(0xFF555555); // texto auxiliar levemente mais escuro
  static const cinzaBorda     = Color(0xFFDDDDDD); // bordas de cards e containers
  static const fundoPagina    = Color(0xFFF5F5F5); // cor de fundo geral das páginas
}